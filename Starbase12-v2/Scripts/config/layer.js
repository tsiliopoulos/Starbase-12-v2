var layer;
(function (layer) {
    // Game Layers
    layer.BACKGROUND = 0;
    layer.HUD = 1;
    layer.PHASER = 2;
    layer.PHOTON = 3;
    layer.PLAYER_SHIELD = 4;
    layer.PLAYER = 5;
    layer.CROSSHAIR = 10;
})(layer || (layer = {}));
//# sourceMappingURL=layer.js.map